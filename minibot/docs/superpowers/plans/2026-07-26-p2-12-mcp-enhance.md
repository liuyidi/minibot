# P2-12 · MCP 增强 (Plan)

> spec：`specs/2026-07-26-p2-12-mcp-enhance-design.md`  
> 依赖：P2-13 插件系统（提供 user_config / mcp_servers 声明）

## Constraints

- OAuth token 加密存储；不裸文件
- 单进程 keyring 密钥缓存
- 现有 MCP preset 用户不需感知升级

## File map

| File | Role |
|---|---|
| `minibot/mcp/oauth/` | metadata / flow / storage |
| `minibot/mcp/variable_substitution.py` |  |
| `minibot/agent/tools/mcp.py` | 集成 |
| `minibot/mcp/manager.py` | 新（从 mcp.py 抽出） |
| `minibot/api/routes/mcp.py` | 现有扩展 |
| `tests/test_mcp_oauth.py` |  |
| `tests/test_mcp_variable.py` |  |
| `tests/test_mcp_plugin_source.py` |  |
| `tests/test_mcp_workspace_scoped.py` |  |

## Task 1 — 抽 Manager

- [ ] 从 `agent/tools/mcp.py` 抽出 McpManager
- [ ] 缓存 key = (server_key, workspace)
- [ ] 单测：多实例隔离

## Task 2 — 变量替换

- [ ] `substitute(spec, ctx)`
- [ ] 引用不存在报 `misconfigured`
- [ ] 单测

## Task 3 — OAuth

- [ ] discover + authorize + token
- [ ] 加密存储（keyring）
- [ ] 集成到 mcp sdk 的 auth provider
- [ ] 单测：mock http

## Task 4 — 插件源

- [ ] 从 plugin registry 拉 mcp_servers
- [ ] server_key 归一化
- [ ] 单项停用
- [ ] 单测

## Task 5 — Dev API + 文档

- [ ] `/api/dev/mcp` 扩展
- [ ] `docs-plan/phase-p2-12-mcp-enhance.md`

## 验收

- 手工：起一个 OAuth mcp server（如 GitHub MCP）→ 走完授权 → 工具可用
- 手工：两个 workspace 各自启动带 CLAUDE_PROJECT_DIR 的 fs server → 进程隔离
- 手工：`plugin.yaml` 声明 mcp_servers → 启停插件影响 mcp 状态
