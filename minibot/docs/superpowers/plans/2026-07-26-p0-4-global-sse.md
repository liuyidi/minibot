# P0-4 · Global SSE Event Stream (Plan)

> spec：`specs/2026-07-26-p0-4-global-sse-design.md`

## Constraints

- 事件 seq 单调递增，跨重启也不重复
- 心跳保证 nginx / 中间层不主动断
- 慢消费者不阻塞其他订阅

## File map

| File | Role |
|---|---|
| `minibot/bus/global_bus.py` | 新 |
| `minibot/bus/events.py` | Event 类型/枚举 |
| `minibot/api/routes/events.py` | 新路由 |
| `minibot/api/routes/sessions.py` | `/runs` 拆异步 + fallback |
| `minibot/agent/runner.py` | `_emit` → bus |
| `minibot/session/repository.py` | append_event / read_events |
| `minibot/static/devui/*.js` | Dev UI 切单 SSE |
| `tests/test_global_bus.py` |  |
| `tests/test_events_api.py` |  |
| `tests/test_devui_sse.py` |  |

## Task 1 — Event 模型 + Bus

- [ ] Event pydantic 模型
- [ ] `GlobalEventBus.publish/subscribe`
- [ ] Ring buffer + sqlite 持久化
- [ ] backpressure：慢消费者被踢
- [ ] 单测：多 subscriber、过滤、slow consumer

## Task 2 — 路由

- [ ] `GET /api/events` SSE
- [ ] `Last-Event-ID` → `since_seq`
- [ ] 心跳
- [ ] TestClient：订阅、断线重连、filter

## Task 3 — Runner 接线

- [ ] 所有 emit 走 bus
- [ ] `run_started/end`、`delta`、`message`、`tool_call`
- [ ] 单测：脚本化 run → 事件顺序对

## Task 4 — /runs 拆异步

- [ ] `POST /api/runs` 202 + run_id
- [ ] `/api/runs/stream` 保留 fallback
- [ ] `POST /api/runs/{id}/abort` 已有 → 兼容
- [ ] 单测：新流程完整跑通

## Task 5 — Dev UI 切换

- [ ] 首页改用单 SSE
- [ ] Trace 页也复用
- [ ] 手工：多标签页共存，事件都到

## Task 6 — Dev stats + 文档

- [ ] `/api/dev/events/stats`
- [ ] README + `docs-plan/phase-p0-4-global-sse.md`

## 验收

- pytest 全绿
- 两个终端同时订阅 `/api/events`；一处发起 run，两边都能实时看到
- 手动 kill 连接，重连 `Last-Event-ID` 收到缺失事件
