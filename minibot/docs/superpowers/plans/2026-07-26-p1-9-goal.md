# P1-9 · Goal (Plan)

> spec：`specs/2026-07-26-p1-9-goal-design.md`  
> 依赖：P0-1 审批（headless）、P0-3 SQLite、P0-4 SSE

## Constraints

- Resume 只在 `AgentLoop` 空闲时触发，同 session 一把锁
- 退避严格递增；失败到顶自动 blocked
- system prompt 注入必须放在 P0-5 compact 保留区外

## File map

| File | Role |
|---|---|
| `minibot/goals/models.py` | Goal pydantic |
| `minibot/goals/repository.py` | sqlite CRUD |
| `minibot/goals/service.py` | 创建 / 状态迁移 |
| `minibot/goals/resume_coordinator.py` | 订阅 run_end + 触发 |
| `minibot/agent/context.py` | 注入 system prompt |
| `minibot/agent/tools/goal.py` | 5 个工具 |
| `minibot/api/routes/goals.py` |  |
| `minibot/app_state.py` | 启动 coordinator |
| `tests/test_goal_repository.py` |  |
| `tests/test_goal_service.py` |  |
| `tests/test_resume_coordinator.py` |  |
| `tests/test_goal_tools.py` |  |

## Task 1 — 数据 + service

- [ ] Goal 模型 + 状态机
- [ ] service：set/update/complete/block/clear
- [ ] 单测：所有转移合法/非法路径

## Task 2 — Coordinator

- [ ] 订阅 bus run_end
- [ ] 计算 backoff
- [ ] 调用 handle_turn（headless）
- [ ] Progress 计数逻辑
- [ ] 单测：fake bus + fake loop

## Task 3 — Context 注入

- [ ] system prompt 段落
- [ ] compact 保留标记
- [ ] 单测：注入内容

## Task 4 — 工具

- [ ] 5 个 goal 工具
- [ ] 参数校验 & 状态转移
- [ ] 单测

## Task 5 — REST + SSE

- [ ] CRUD 路由
- [ ] 事件
- [ ] TestClient

## Task 6 — Startup recovery + 文档

- [ ] 启动扫描
- [ ] `docs-plan/phase-p1-9-goal.md`

## 验收

- 手工：设目标 "创建 3 个 test file"，模型自动多轮直到完成 → 状态 completed
- 手工：设不可能达成目标 → 到 3 轮 no_progress 自动 blocked
- 手工：暂停/恢复正常
