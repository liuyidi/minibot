# Heartbeat Tasks

<!--
This file is checked periodically by minibot. When heartbeat is enabled
(default: every 1h), a protected system cron job reads this file.

Use this for recurring background checks that should stay quiet unless there
is something useful to report. Regular cron automations are different: they
normally deliver each run's result back to the chat where they were created.

If this file has no tasks (only headers and comments), the agent will skip it.
Completed tasks should be deleted — heartbeat only reads "Active Tasks".
-->

## Active Tasks

<!-- Add your periodic tasks below this line -->
