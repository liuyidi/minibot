{
  "name": "minibot-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:cli": "npm --workspace packages/minibot-cli run build",
    "test:cli": "npm --workspace packages/minibot-cli run test",
    "lint": "npm run build:cli && npm run test:cli"
  }
}
