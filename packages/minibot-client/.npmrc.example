# GitHub Packages — private @liuyidi/* scope (published name)
# Apps import as `@minibot/client` via npm alias; auth still uses @liuyidi.
@liuyidi:registry=https://npm.pkg.github.com
# //npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
