{
  "name": "liuyidi-me-site",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "node scripts/extract-changelog.mjs && vitepress dev",
    "docs:build": "node scripts/extract-changelog.mjs && vitepress build",
    "docs:preview": "vitepress preview",
    "docs:check": "node scripts/check-dist.mjs"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
