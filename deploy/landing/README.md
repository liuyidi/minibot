# Landing page for https://liuyidi.me

Served by host Nginx from this directory on the Aliyun ECS that also runs minibot.

```text
root /opt/demo/minibot/deploy/landing;
```

See `../nginx.liuyidi.me.conf.example` and `../README.md`.

Previews (do not replace `index.html` until approved):

- V0.1 Option A → https://liuyidi.me/v0.1/ (see `v0.1/`)
