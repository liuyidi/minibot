# Bundled minibot sidecar (PyInstaller onedir)

Generated by `desktop/scripts/sidecar/prepare-sidecar.sh` from
`dist/sidecar/<triple>/minibot-sidecar/`.

Do not commit the binary tree (`minibot-sidecar/`). Keep this README tracked.
